    apiVersion: v1
    kind: Service
    metadata:
      name: minio-service
    spec:
      selector:
        app: minio
      ports:
        - name: console
          protocol: TCP
          port: 9000
          targetPort: 9000
          nodePort: 30000
      type: NodePort # Or NodePort/LoadBalancer for external access
