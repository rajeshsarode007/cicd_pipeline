    apiVersion: v1
    kind: PersistentVolumeClaim
    metadata:
      name: minio-pvc
    spec:
      storageClassName: minio            
      accessModes:
        - ReadWriteOnce
      resources:
        requests:
          storage: 4Gi # Adjust storage size as needed
